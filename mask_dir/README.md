# Calibrated sensing mask

Place the calibrated sensing mask at:

```text
mask_dir/6504pro_10x_flipud.mat
```

The MAT file must contain a `mask` array whose first dimension contains the 46 spectral channels expected by the current S2RNet configuration.

