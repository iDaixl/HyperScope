# Pretrained model

Place the S2RNet checkpoint at:

```text
model_zoo/net.pth
```

Model checkpoints are excluded from Git because they are large, experiment-specific binary artifacts. Update `model_path` in `test_inference_with_unmixing.py` if you use a different filename.

